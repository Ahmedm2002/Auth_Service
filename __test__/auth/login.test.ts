import jest from "jest";
import supertest from "supertest";
import { app } from "../../src/app.js";

describe("Login test", () => {
  it("Should return 200 when created");
  test("Should return 200 when created", () => {});
});
